<%= form_with(model: custom_field_option) do |form| %>
  <% if custom_field_option.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(custom_field_option.errors.count, "error") %> prohibited this custom_field_option from being saved:</h2>

      <ul>
        <% custom_field_option.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :value %>
  </div>

  <div>
    <%= form.label :custom_field_id, style: "display: block" %>
    <%= form.text_field :custom_field_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
